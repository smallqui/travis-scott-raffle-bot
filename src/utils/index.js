const { entryPayload } = require("./payload");
const { extractHandle, extractProduct, extractSizes } = require("./extract")

module.exports = { entryPayload, extractHandle, extractProduct, extractSizes };